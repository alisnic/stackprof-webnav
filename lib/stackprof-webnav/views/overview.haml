%h3 StackProf Navigator - #{@action} (#{@frames.count} frames)
%hr

%p
  Viewing dump
  %b= @file

- if current_report.data[:raw]
  %a{href: flamegraph_url}
    %button.btn View flamegraph

%table.centered
  %thead
    %th Total
    %th %
    %th Samples
    %th %
    %th Method

  %tbody
    - @frames.each do |frame|
      %tr
        %td= frame[:total]
        %td= frame[:total_pct]
        %td= frame[:samples]
        %td= frame[:samples_pct]
        %td
          %a{:href => method_url(frame[:method])}
            = frame[:method]
